//! User configuration and key bindings.
//!
//! Loads settings from the user's config directory (audio device choice,
//! key bindings, palette, default download location), falling back to
//! sensible defaults. The config dir doubles as the runtime store, kept
//! out of version control via `.termkrush/` in `.gitignore`.
//!
//! Placeholder until the config story lands.

/// Load configuration from disk, or return defaults if none exists.
///
/// Placeholder: real parsing + defaults arrive with the config story.
pub fn load() {
    // intentionally empty for the scaffold
}
