//! A deck: one loaded track and its transport state.
//!
//! A deck owns playback position, play/pause, pitch/tempo, cue and loop
//! points, and the keyboard-jog scratch state over marked zones. The mix
//! module combines two-to-four decks onto the master bus.
//!
//! Placeholder until the one-deck playback story lands.

/// Create an empty deck with no track loaded.
///
/// Placeholder: real transport state arrives with the one-deck story.
pub fn new_deck() {
    // intentionally empty for the scaffold
}
