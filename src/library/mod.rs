//! Track library: local storage, downloads, and metadata.
//!
//! Tracks are pulled into a local store from direct URLs and via a
//! `yt-dlp` shell-out, then decoded and analyzed (BPM, duration). The
//! library is the source the TUI browser lists and decks load from.
//!
//! Placeholder until the download story lands.

/// Open (or create) the local track library under the user's data dir.
///
/// Placeholder: real storage + indexing arrive with the download story.
pub fn open() {
    // intentionally empty for the scaffold
}
